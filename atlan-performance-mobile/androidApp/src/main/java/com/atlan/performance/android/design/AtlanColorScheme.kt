package com.atlan.performance.android.design

import androidx.compose.ui.graphics.Color
import com.atlan.performance.shared.design.AtlanColors

/**
 * Compose Color mirror of the shared token table (DESIGN_TOKENS.md). Coral is rare; Tide marks
 * science / calm progress / selected state. Never use Coral or red for disruption.
 */
object AtlanPalette {
    val Abyss = Color(AtlanColors.Abyss)
    val AbyssDeep = Color(AtlanColors.AbyssDeep)
    val Tide = Color(AtlanColors.Tide)
    val TideDeep = Color(AtlanColors.TideDeep)
    val TideSoft = Color(AtlanColors.TideSoft)
    val TidePale = Color(AtlanColors.TidePale)
    val Coral = Color(AtlanColors.Coral)
    val CoralBright = Color(AtlanColors.CoralBright)
    val CoralDeep = Color(AtlanColors.CoralDeep)
    val Foam = Color(AtlanColors.Foam)
    val FoamWarm = Color(AtlanColors.FoamWarm)
    val Paper = Color(AtlanColors.Paper)
}
